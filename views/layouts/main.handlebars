<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>TrailBlazer</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0, user-scalable=no"/>
    <title>Emily Sun Portfolio</title>
    <!--Jquery-->
    <script src="https://code.jquery.com/jquery-2.2.3.js" integrity="sha256-laXWtGydpwqJ8JA+X9x2miwmaiKhn8tVmOVEigRNtP4=" crossorigin="anonymous"></script>
     <script type="text/javascript" src="/assets/js/materialize.min.js"></script>

    <!--javascript file-->
    <script type="text/javascript" src="/assets/js/js.js"></script>
    <!-- CSS  -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Roboto:900" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Poppins" rel="stylesheet">

    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.98.0/css/materialize.min.css">
    <link href="/assets/css/style21.css" type="text/css" rel="stylesheet" media="screen,projection"/>
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>
  {{!-- <ul id="dropdown1" class="dropdown-content">
    <li><a href="#!">{{user.username}}</a></li>
    <li><a href="/auth/facebook">Log In</a></li>
    <li><a href="/signup">Sign Up</a></li>
  </ul>
  <nav class="N/A transparent z-depth-0">
    <div class="nav-wrapper">
      <!-- <a href="#!" class="brand-logo">Logo</a> -->
      <ul class="right hide-on-med-and-down">
        <!-- Dropdown Trigger -->
        <li><a class="dropdown-button" href="/" data-activates="dropdown1">Account Access<i class="material-icons right">arrow_drop_down</i></a></li>
      </ul>
    </div>
  </nav> --}}
	{{{ body }}}
    {{!-- Modal --}}
    <!-- Modal Structure -->
    <div id="loginModal" class="modal">
      <a href="#!" class=" modal-action modal-close waves-effect waves-green btn-flat right"><i class="large material-icons" >toc</i></a>
      <!-- Modal Content -->
      <div class="modal-content" id="loginModalContent">
        <!--Beginning of Google Maps ADDED-->
        <div class="row" id="introRow">
          <div class="col s12">
            <h5>Log Into to your Account to:</h5>
            <p>Save trails to your account</p>
            <p>View your favourite trails</p>
          </div>
        </div>

        <div class="row" id="loginRow">
          <div class="col s6" id="manualInput">
            <form id="loginForm">
              <div class="row">
                <div class="input-field">
                  <input id="name" type="text" class="validate" required="required">
                  <label for="name">Username</label>
                </div>
                <div class="input-field">
                  <input id="name" type="text" class="validate" required="required">
                  <label for="name">Password</label>
                </div>
                <a class="waves-effect waves-light btn" href="#modal1"  id = "submit">Submit</a>
              </div>
            </form>
          </div>
          {{!-- facebook column --}}
          <div class="col s6 center-align" id="facebookLogin">
            <p>Login With Facebook</p>
            <a href="/auth/facebook" class="btn btn-primary blue darken-3" id="#facebookSubmit"><span class="fa fa-facebook"></span> Continue with Facebook </a>
          </div>
        </div>
        {{!-- footer --}}
        <div class="row" id="footerRow">
          <div class="col s12">
            <p>Don't Have an account? &nbsp;<span><a href="/signup">Register here</a></span></p>
          </div> 
        </div>
      </div> 
    </div>
    {{!-- End Of Modal --}}
</body>
  <script type="text/javascript">
    //=====================Map=====================
    var map;
    // var latitude = ParseInt($(".latitude").val());
    // var longitude= ParseInt($(".longitude").val());
    function initMap() {
      console.log('in initMap', document.getElementById('map'));
      map = new google.maps.Map(document.getElementById('map'), {
        // center: {lat:latitude, lng: longitude},
        center: {lat: -34.397, lng: 150.644},
        //Need to find someway to grab latitude and longtitude
        zoom: 11
        });
      infoWindow = new google.maps.InfoWindow();
    };

    // google.maps.event.addDomListener(window, 'load', initialize);
    // google.maps.event.addDomListener(window, "resize", function() {
    //  var center = map.getCenter();
    //  google.maps.event.trigger(map, "resize");
    //  map.setCenter(center); 
    // });
    
    $(document).ready(function(){
      $('.parallax').parallax();
      $('.modal').modal();
      $('select').material_select();
      $(".dropdown-button").dropdown();
      //hides parkDisplay on first display
      // $("#parkDisplay").hide();
      //hides parkDisplay on first display
      // $("#parallax2").hide();
      $( "#submitBtn" ).click(function() {
     		console.log($('#stateSelection').val())
        $("#parkDisplay").show();;
      });
      $(".parkSubmitBtn").click(function(){
        $("#parallax2").show();
      });
    });     
  </script>
  <script type ="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBtd0Rm6EoowNo16BoCBpQwxqgv40Z5P0U&libraries=places&callback=initMap"></script>
	</html>